# SECRETS — never commit. This repo is public.
config/*.key
config/*.secret
.env
*.env

# Market data. 177MB of parquet, and the poll fetches live data anyway, so
# there is nothing here CI needs.
data/store/
data/cache/
*.parquet

# Local run output
results/
state/demo_log.txt.bak
scratchpad/
*.log

# Backups made while editing
# any backup suffix, not a hand-kept list. strategy.py.bak5 slipped into a
# public commit because the list stopped at bak4.
*.bak*

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Dead local launchers, replaced by GitHub Actions
run_demo.bat
run_demo.ps1
